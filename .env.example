# Публичные переменные сборки (можно в git и в GitHub Actions → Variables).
# СЕКРЕТЫ сюда не писать (токены бота, БД, пароли).
# Локальные секреты для будущего VPS: ПРОЧЕЕ/secrets.env (папка в .gitignore).

# Свой домен на корне — BASE_PATH пустой.
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_SITE_URL=https://beri36.ru
NEXT_PUBLIC_YANDEX_METRIKA_ID=
NEXT_PUBLIC_YANDEX_VERIFICATION=
